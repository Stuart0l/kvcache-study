# Toy KV-Cache Demo

A minimal decoder-only (GPT-style) transformer implemented from scratch in
PyTorch, used to study how KV caching works by comparing autoregressive
generation with and without a KV cache.

See `PLAN.md` for the implementation plan.
